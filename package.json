{
  "name": "jugnibishnoi",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w client && node scripts/mirror-dist.js",
    "preview": "npm run preview -w client",
    "test": "node client/src/game/engine.test.mjs",
    "deploy": "npx vercel --prod",
    "deploy:preview": "npx vercel"
  },
  "engines": {
    "node": ">=20"
  }
}
